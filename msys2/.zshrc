
fpath=("~/gitstuff/zsh-completions/src" $fpath)
# The current completion script for curl is super minimal and needs some work

# Temporary vim alias so it recognizes ~/.vimrc
alias vim=vim -u /c/Users/Carl/.vimrc

